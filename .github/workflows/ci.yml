name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  rust:
    name: Rust — fmt, clippy, test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Lint (clippy)
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

      - name: Run tests
        run: cargo test --workspace --all-features

      - name: Check feature isolation
        run: |
          cargo check -p rieko-cli --no-default-features
          cargo check -p rieko-cli --no-default-features --features simulate
          cargo check -p rieko-cli --no-default-features --features execute

      - name: Build
        run: cargo build --workspace

      - name: Verify no executor in default build
        run: |
          cargo check -p rieko-cli --no-default-features --features simulate
          echo "rieko-execution crate must not appear in default dependency tree"
          ! cargo tree -p rieko-cli --no-default-features --features simulate 2>/dev/null | grep -q 'rieko-execution'

  frontend:
    name: Frontend — build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Check types
        run: npm run check

      - name: Build
        run: npm run build
